# Share declarative configuration by projection

Identity Profiles will share versioned, immutable configuration intent rather than live writable Codex directories. CodexFolio will render validated Shared Configuration Packs into each Identity Home with local overrides, while credentials, OAuth grants, sessions, SQLite state, logs, caches, and mutable installation state remain profile-owned.
