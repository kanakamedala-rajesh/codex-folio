# Build a companion application

The product will be a local companion application combining a CLI launcher with a responsive local web dashboard. A Codex plugin alone cannot select an identity before Codex starts, while a dashboard alone cannot provide terminal-only switching; the companion must support the complete workflow without requiring a GUI and may add an optional Codex plugin integration later.
